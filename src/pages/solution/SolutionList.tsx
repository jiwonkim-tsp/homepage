import {
  $List,
  $ImgBox,
  $ProductInfo,
  $Title,
  $Content,
  $Sentence,
  $ColoredText,
} from "./styleList";
import { IListProps } from "../products/ProductList";

const SolutionList = ({ order, title, img, sentence }: IListProps) => {
  return <div>SolutionList</div>;
};

export default SolutionList;
